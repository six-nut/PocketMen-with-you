# Launch plan — v0.3.0 Neural Local Studio

## Hook

**Two photos in. A high-fidelity animated Codex companion out. Locally. No OpenAI API key.**

## Demo priorities

1. human → premium `hero-chibi`;
2. real pet → `soft-real` with eye/fur/accessory fidelity;
3. owner + pet multi-reference companion;
4. original creature + PocketMen Companion Capsule;
5. side-by-side `v0.2 deterministic` vs `v0.3 neural-local` contact sheets.

Do not use copyrighted franchise characters as launch assets. Use creator-owned or consented demo references.

## GitHub release message

Lead with the architecture breakthrough, not model names:

- true local generative state poses;
- multi-reference identity preservation;
- no hatch-pet dependency;
- no OpenAI API key;
- deterministic fallback;
- commercial-friendly default model license.

## Star-ready proof points

- one-command skill installation;
- `pocketmen doctor` hardware guidance;
- reproducible QA artifacts;
- privacy-first defaults;
- clear model licensing;
- small Git repo because weights stay in external caches.
